SUPABASE_URL=https://jfrvoykwowzthlvzwsmi.supabase.co
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_ANON_KEY=
# Public origin allowlist for password links and Google OAuth returns.
# Never place provider client secrets here; configure them only in Supabase.
AUTH_SITE_URL=https://nschessn.vercel.app
# Optional extra reviewed origins, comma-separated (for example a localhost
# development origin or a specific staging URL). Do not use broad wildcards.
AUTH_REDIRECT_ORIGINS=
# Server-side only. Never expose this value in index.html or client-side code.
SUPABASE_SERVICE_ROLE_KEY=
